---
# RKNN C API multi-thread benchmark — ResNet18 INT8, 3 threads
# 3 concurrent rknn_context threads, each pinned to a distinct NPU core
# (thread 0→CORE_0, 1→CORE_1, 2→CORE_2).  All threads start simultaneously
# via pthread barrier.
# Expected aggregate: ~3× single-thread = ~430 fps.
apiVersion: batch/v1
kind: Job
metadata:
  name: rknn-bench-npu-c-mt-resnet18-t3
  namespace: default
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: bench
          image: ghcr.io/schwankner/rk3588-rknn-bench:v15
          imagePullPolicy: Always
          command: ["/bench_c_mt", "/model/resnet18.rknn", "resnet18", "3", "1000", "50"]
          env:
            - name: PYTHONUNBUFFERED
              value: "1"
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          resources:
            limits:
              rockchip.com/npu: "1"
              cpu: "4"
              memory: 512Mi
            requests:
              cpu: "2"
              memory: 256Mi
