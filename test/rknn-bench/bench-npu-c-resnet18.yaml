---
# RKNN C API benchmark Job — ResNet18 INT8
#
# Uses the native C API (rknn_init/rknn_run/rknn_outputs_get) directly,
# bypassing the 3-5 ms Python/rknnlite per-call overhead to show true NPU
# throughput.  Expected: ~500-1000 fps (vs ~137 fps with Python overhead).
#
# This job runs /bench_c (compiled in the v13 image), not bench.py.
apiVersion: batch/v1
kind: Job
metadata:
  name: rknn-bench-npu-c-resnet18
  namespace: default
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: bench
          image: ghcr.io/schwankner/rk3588-rknn-bench:v15
          imagePullPolicy: Always
          # Override ENTRYPOINT to run the C binary directly.
          command: ["/bench_c", "/model/resnet18.rknn", "resnet18", "2000", "50"]
          env:
            - name: PYTHONUNBUFFERED
              value: "1"
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          resources:
            limits:
              rockchip.com/npu: "1"
              cpu: "2"
              memory: 512Mi
            requests:
              cpu: "1"
              memory: 256Mi
