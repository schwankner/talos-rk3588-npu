---
# RK3588 NPU Device Plugin DaemonSet
#
# Runs on every node labeled rockchip.com/npu-capable=true.
# Discovers the NPU DRM device nodes dynamically via sysfs:
#   /sys/bus/platform/drivers/RKNPU/fdab0000.rknpu/drm/
# Registers rockchip.com/npu with the kubelet and writes the CDI spec to
# /var/run/cdi/rockchip-npu.yaml so containerd injects the DRM render node,
# card node, dma_heap, and librknnrt.so into requesting containers.
#
# Label NPU-capable nodes before deploying:
#   kubectl label node <node-name> rockchip.com/npu-capable=true
#
# Usage in a pod:
#   spec:
#     containers:
#       - resources:
#           limits:
#             rockchip.com/npu: "1"
#
# Up to 3 pods can hold rockchip.com/npu simultaneously (one per NPU core).
# All slots share /dev/rknpu; the RKNN runtime distributes work across cores.
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: rk3588-npu-device-plugin
  namespace: kube-system
  labels:
    app: rk3588-npu-device-plugin
spec:
  selector:
    matchLabels:
      app: rk3588-npu-device-plugin
  template:
    metadata:
      labels:
        app: rk3588-npu-device-plugin
    spec:
      priorityClassName: system-node-critical
      tolerations:
        - operator: Exists
          effect: NoSchedule

      nodeSelector:
        kubernetes.io/arch: arm64

      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: rockchip.com/npu-capable
                    operator: In
                    values: ["true"]

      containers:
        - name: rk3588-npu-device-plugin
          image: ghcr.io/schwankner/rk3588-npu-device-plugin:85b1a0c
          imagePullPolicy: Always

          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]

          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 64Mi

          volumeMounts:
            - name: device-plugin
              mountPath: /var/lib/kubelet/device-plugins
            - name: dev
              mountPath: /dev
            - name: sys
              mountPath: /sys
              readOnly: true
            - name: cdi
              mountPath: /var/run/cdi
            - name: lib
              mountPath: /usr/lib
              readOnly: true
            - name: etc-cdi
              mountPath: /etc/cdi
              readOnly: true

      volumes:
        - name: device-plugin
          hostPath:
            path: /var/lib/kubelet/device-plugins
            type: Directory
        - name: dev
          hostPath:
            path: /dev
            type: Directory
        - name: sys
          hostPath:
            path: /sys
            type: Directory
        - name: cdi
          hostPath:
            path: /var/run/cdi
            type: DirectoryOrCreate
        - name: lib
          hostPath:
            path: /usr/lib
            type: Directory
        - name: etc-cdi
          hostPath:
            path: /etc/cdi
            type: Directory
